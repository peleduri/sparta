name: Sparta - Example Caller

on:
  pull_request:
    branches:
      - main
      - master
      - develop

jobs:
  vulnerability-scan:
    name: Run Vulnerability Scan
    uses: ./.github/workflows/sparta-package-vulnerability-scan.yml
    # Optional: Override default inputs
    # with:
    #   scan-ref: '.'
    #   severity: 'CRITICAL,HIGH'
    #   exit-code: '1'

